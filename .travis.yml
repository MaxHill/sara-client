language: node_js
node_js:
  - "4.0"
before_script:
  - npm install -g gulp
  - gulp --production
after_success:
  - surge --project ./public --domain sara.hilloco.se
branches:
  only:
    - production
